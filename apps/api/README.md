# API application

Symfony modular monolith. C1 introduces the runtime skeleton; C2 implements
Shared, Places, Discovery, and Administration. Doctrine entities remain private
and public operations use explicit DTOs under `/api/v1`.
