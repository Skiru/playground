# Web application

React Router Framework Mode application with SSR. Public catalogue pages must
remain useful as HTML without JavaScript; maps are client-only progressive
enhancements with an equivalent text list.
