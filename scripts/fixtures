#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_dir"
docker compose -f compose.yaml -f compose.dev.yaml run --rm api php bin/console doctrine:fixtures:load --no-interaction
