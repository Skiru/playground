#!/bin/bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
. "$SCRIPT_DIR/lib.sh"

require_env
compose ps
current_desc="$(release_descriptor)"
if [ -f "$current_desc" ]; then
  printf '\nCurrent Release Descriptor:\n'
  cat "$current_desc"
fi
