#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_dir"
[ -f .env ] || { printf '%s\n' 'Missing .env. Run ./scripts/bootstrap first.' >&2; exit 1; }
docker compose -f compose.yaml -f compose.dev.yaml --profile dev up --build
