# Container images

Multi-stage Debian-based FrankenPHP and Node SSR image definitions belong here.
Production runtime stages exclude development dependencies and run non-root
where supported by the selected images.
