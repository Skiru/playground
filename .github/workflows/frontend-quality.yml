name: Frontend quality

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: frontend-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install pnpm workspace
        run: docker run --rm -u "$(id -u):$(id -g)" -e HOME=/tmp -v "$PWD:/workspace" -w /workspace node:24-bookworm sh -c 'corepack pnpm install --frozen-lockfile'
      - name: Frontend checks
        run: docker run --rm -u "$(id -u):$(id -g)" -e HOME=/tmp -v "$PWD:/workspace" -w /workspace node:24-bookworm sh -c 'corepack pnpm --filter @family-places/api-client build && corepack pnpm --filter @family-places/web lint && corepack pnpm --filter @family-places/web typecheck && corepack pnpm --filter @family-places/web test && corepack pnpm --filter @family-places/web build'
      - name: Build PHP 8.5 OpenAPI runtime
        run: docker build --target development -t family-places-api:development -f infra/docker/api.Dockerfile .
      - name: Generated client drift
        run: ./scripts/api-check
